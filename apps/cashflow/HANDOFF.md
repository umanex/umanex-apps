# HANDOFF.md — sessie-handoff (vooruitkijkend)

Dit bestand is de **vooruitkijkende tegenhanger** van `LEARNINGS.md`. Waar LEARNINGS de rauwe vangst van *fouten* is, houdt HANDOFF de open **onzekerheden, aannames, risico's, next steps en ideeën** bij die een sessie achterlaat — zodat een volgende sessie niet koud begint.

Entries komen erbij via de `sessie-reflectie` skill aan het einde van een sessie. De open items worden bij de start van een volgende sessie automatisch getoond via de user-level SessionStart-hook (`~/.claude/hooks/session-start-handoff.sh`). Niet handmatig bewerken tenzij je een status corrigeert.

## Waarom dit bestaat

Aan het einde van een sessie zit de meeste context in het hoofd van Claude en verdampt bij afsluiten: waar was ik het minst zeker over, welke aanname bleef onuitgesproken, wat breekt over 3 maanden, wat is de eerste zet volgende keer. HANDOFF vangt dat expliciet op zodat het meekomt.

Dit is **geen duplicaat van de eval-loop**. Een terugkerende *faalklasse* hoort in `LEARNINGS.md` (via `vastleggen`); een *durend feit* hoort in auto-memory. HANDOFF is enkel voor het vooruitkijkende, sessie-gebonden restant.

## Statussen

- `open` — vastgelegd bij reflectie, nog niet opgepakt. Wordt bij sessiestart getoond.
- `resolved` — opgepakt of beantwoord in een latere sessie; blijft staan als spoor, wordt niet meer getoond.

## Types

`onzekerheid` · `aanname` · `risico` · `next-step` · `idee` · `debt`

## Format

Elke entry staat onder een laag-header (`# Globaal`, `# Klant — {naam}`, `# Project — {app}`) en heeft deze vorm:

    ## YYYY-MM-DD — {korte titel} · [{type}]
    - **Bevinding:** {1-2 zinnen}
    - **Volgende zet:** {concreet actiepunt of "-"}
    - **Status:** open

<!-- De sessie-reflectie skill voegt hieronder de juiste laag-header toe bij de eerste entry. -->

# Project — cashflow

## 2026-08-05 — Beginsaldo aanklikken en wegklikken wist een bestaande correctie · [risico]
- **Bevinding:** `MonthCard.tsx` beslist of een ingevoerd beginsaldo een override wordt door het te vergelijken met `useComputedStartBalance()` — maar die waarde bevat de actieve override zelf, want `computeAnchorState` geeft een override letterlijk terug. De guard vraagt dus "heb je iets veranderd?" in plaats van "is dit wat de app zelf zou berekenen?". `StartBalanceRow` vuurt `onBlur` ook zonder toetsaanslag, dus één klik op het bedrag gevolgd door een klik ernaast verwijdert de correctie en laat de hele prognose van drie maanden verschuiven. Escape is wél veilig. Pre-existing, bevestigd en gereproduceerd door het reviewpanel van 2026-08-05.
- **Volgende zet:** Een tweede afleiding nodig: "wat zou het beginsaldo zijn zónder override". Ofwel `computeAnchorState` laten teruggeven wat het berekende náást de override, ofwel een aparte hook die de overrides eruit filtert. Daarna in `MonthCard.tsx` daartegen vergelijken. Vergeet niet dat `useMonths` en `useComputedStartBalance` op hetzelfde anker draaien — de fout is niet zichtbaar zolang je beide zijden uit dezelfde bron haalt.
- **Status:** resolved — 2026-08-05 in PR #187: `AnchorState` draagt nu twee getallen, `startBalance` (correctie inbegrepen) en `computedStartBalance` (wat de app zelf uitrekent). De maandkaart vergelijkt tegen het tweede en doet niets wanneer er niets veranderd is. Vastgelegd in `scripts/anchor-scenarios.ts` (A9).

## 2026-08-05 — Ankermaand telt betaalde posten niet van het gesimuleerde banksaldo af · [debt]
- **Bevinding:** Maand 0 behandelt alles wat al betaald is als "er al af" en rekent alleen wat nog moet vertrekken. Maar het banksaldo dat `computeAnchorState` fabriceert is het vrije saldo aan het *begin* van de ankermaand plus de potten — daar zijn de betalingen van díé maand nog niet af. Bij een betaalde vaste kost, een betaalde uitgave of een potbetaling in de ankermaand valt het eindsaldo daardoor te hoog uit, precies met dat bedrag. Pre-existing en breder dan de buffer: het raakt elke pot. Zichtbaar zodra je een maand terugbladert waarin je posten had afgevinkt.
- **Volgende zet:** In `computeAnchorState` het gesimuleerde banksaldo verlagen met alles wat in de ankermaand als betaald geregistreerd staat (betaalde `recurringSettlements`, betaalde `expenseItems`, `fromReservation` + `fromCash` van de betalingen van die maand). Eerst vastleggen in `scripts/anchor-scenarios.ts` — het harnas heeft nu bewust geen betaalde posten in de ankermaand, dus de invariant is daar nog blind voor. Let op: dit mag níét gelden voor het pad waar de gebruiker zelf een banksaldo invulde (`referenceBalance` of een `balanceOverride`), want dat is al de echte stand.
- **Status:** resolved — 2026-08-05 in PR #189: `bankFromFree` trekt alles af wat in de ankermaand betaald geregistreerd staat — betaalde vaste kosten, betaalde uitgaven en beide poten van een potbetaling. De uitzondering uit de "volgende zet" klopt en is gerespecteerd: het pad waar jij zelf een saldo invulde (`referenceBalance` of een correctie) raakt de aftrek niet. Vastgelegd in A10 en A11.

## 2026-08-05 — Gefinaliseerde pot in de ankermaand laat zijn saldo verdwijnen · [risico]
- **Bevinding:** In `computeAnchorState` filtert `reservedAtAnchorStart` (`lib/cashflow/calculator.ts`, rond regel 760) gefinaliseerde potten weg, terwijl het opgebouwde saldo van die pot in eerdere maanden wél van het vrije saldo is afgetrokken. Dat saldo komt dan nergens meer terug: met een spaarpot van €4.200 die in de ankermaand gefinaliseerd wordt, geeft het venster vanaf januari €2.500 eindsaldo en het venster rechtstreeks op de ankermaand −€1.700. Pre-existing en los van de buffer, maar de sweep vergroot de impact: het beginsaldo van de ankermaand bestaat nu volledig uit potstanden, dus slaat een fout daar meteen door naar alles.
- **Volgende zet:** Uitzoeken of de `!p.finalized`-filter daar terecht is. Vermoedelijk moet een pot die in de ankermaand gefinaliseerd wordt zijn `deferredFromPrevious` wél terugkrijgen, omdat `computeMonthSubtotals` diezelfde pot in maand 0 volledig overslaat en de vrijgave dus nooit boekt. Eerst een scenario in `scripts/buffer-scenarios.ts` dat het vastlegt, dan pas fixen.
- **Status:** resolved — 2026-08-05: het vermoeden klopte. De oorzaak zat dieper dan de filter: het banksaldo werd uit een andere bron opgeteld dan de potten-map die de functie teruggeeft. Beide komen nu uit dezelfde map (`bankFromFree`), waardoor de filter overbodig werd. Vastgelegd in `scripts/anchor-scenarios.ts` (A2, A3).

## 2026-08-05 — Twee gaten in de snapshot-tak van computeAnchorState · [debt]
- **Bevinding:** Twee bevindingen uit het reviewpanel van de buffer-sweep, allebei in de snapshot-tak van `computeAnchorState` en allebei vandaag onbereikbaar omdat `cashflow_snapshots` nog leeg is. (1) Een `balanceOverride` op een maand tússen de laatst afgesloten maand en de ankermaand wordt genegeerd — die tak zoekt alleen een override op de ankermaand zelf, de niet-snapshot-tak gebruikt wél de meest recente eerdere. (2) De overbruggingstak valt terug op de potstanden bij afsluiting wanneer geen enkel spaardoel in de ankermaand aangerekend wordt (bv. door een uitstel), waardoor een tussentijdse opbouw verdwijnt.
- **Volgende zet:** Oppakken vóór de eerste maand afgesloten wordt — vanaf dan wordt dit pad wél gebruikt en zijn het stille rekenfouten in het banksaldo. Beide zijn te vangen met een scenario in `scripts/buffer-scenarios.ts` naast S18.
- **Status:** resolved — 2026-08-05: (1) een correctie ná de laatst afgesloten maand overbrugt nu in twee etappes: eerst tot aan de correctie voor de bevroren potstanden, dan verder met het gecorrigeerde saldo. De correctie vervangt dus het saldo maar niet de historie (A4, A8). (2) De potstanden komen uit twee lagen: de eindstand van de maand vóór het anker, daarna wat de ankermaand zelf meldt, zodat een vertrokken pot zijn opbouw houdt (A5, A7). Beide in `scripts/anchor-scenarios.ts`.

## 2026-08-04 — Runway leest de ankermaand, niet vandaag · [risico]
- **Bevinding:** `/analyse` haalt de bufferstand uit `useMonths(3)[0]`, en die volgt `anchorMonth` uit de store. Navigeer je op de prognosepagina een maand terug en ga je dan naar de analyse, dan rekent de runway met de bufferstand van die andere maand — zonder dat de pagina dat zegt. De navigatie is sessie-state, dus het verdwijnt na een herlaad, wat het juist verraderlijk maakt.
- **Volgende zet:** In `app/analyse/page.tsx` de bufferstand halen uit de maand die gelijk is aan `getCurrentMonthKey()` in plaats van uit `months[0]`, of `useMonths` daar aanroepen met een eigen anker. Daarna controleren dat het getal niet meer verandert door op `/` terug te bladeren.
- **Status:** resolved — 2026-08-04: `useMonths` en `useAnchorState` nemen een optionele anker-maand, en `/analyse` geeft daar `getCurrentMonthKey()` aan mee. De analyse rekent dus altijd vanaf vandaag, ongeacht waar je op de prognosepagina staat.

## 2026-08-04 — Twee PR's staan open en zijn niet geverifieerd op scherm · [next-step]
- **Bevinding:** PR #172 (grafieken als inline SVG) en PR #173 (historie begint bij de huidige maand) zijn gebouwd, groen door build/lint/type-check en harness, maar niet gemerged. Van #172 zijn de waterfall en de bufferopbouw wél visueel gecontroleerd en gefixt; de bullet charts (`VarianceChart`) zijn nooit op scherm gezien. Van #173 is niets visueel gecontroleerd. Precies de fout die de visuele controle van #172 blootlegde — een SVG van 672px hoog — compileerde probleemloos.
- **Volgende zet:** #173 eerst mergen (correctheid: hij gooit de niet-vertrouwde historie weg), dan #172. Vóór het mergen van #172 met drie afgesloten maanden demodata naar `/analyse` kijken, in het bijzonder naar de bullet charts en naar een waterfall met een negatief eindsaldo — dat geval is nooit getekend.
- **Status:** resolved — 2026-08-04: #172 en #173 zijn gemerged op verzoek. De visuele controle van de bullet charts en van de historie-vanaf-nu blijft openstaan en verhuist naar de entry hieronder.

## 2026-08-04 — Alle data leeft in één browser, zonder export · [risico]
- **Bevinding:** De store staat in `localStorage` onder `cashflow-store-v3`, per browser en per profiel. Deze sessie bleek dat concreet: in Chrome was alles leeg terwijl Arc de echte cijfers had. Er is geen export of import, dus browserdata wissen of van machine wisselen betekent alles kwijt. Voor een tool waar je financiële planning in staat is dat een enkelvoudig faalpunt.
- **Volgende zet:** Een export- en importknop die de hele store als JSON-bestand wegschrijft en terugleest, met een versiecontrole op `STORE_VERSION`. Klein werk, en het maakt ook het overzetten tussen browsers en het bewaren van een back-up vóór een migratie mogelijk.
- **Status:** resolved — 2026-08-04: opgegaan in de bredere entry over opslag, back-up en meerdere browsers onderaan.

## 2026-08-04 — Automatisch afsluiten kan een verkeerd beginsaldo bevriezen · [risico]
- **Bevinding:** Zodra een maand voorbij is, sluit de app hem bij het eerstvolgende bezoek zichzelf af. Het model gaat ervan uit dat het beginsaldo van de ankermaand je échte banksaldo is, maar niets in de UI herinnert je eraan dat bij te werken. Was dat saldo verouderd op het moment van afsluiten, dan staat die fout permanent in de historie — en van daaruit rolt hij door naar elke volgende maand.
- **Volgende zet:** Overwegen om vóór het automatisch afsluiten te vragen of het beginsaldo klopt, of de afsluiting pas te doen bij het eerste bezoek ná een expliciete bevestiging. Alternatief: bij een heropening tonen hoeveel het bevroren eindsaldo afwijkt van een herberekening, zodat een scheve maand opvalt.
- **Status:** resolved — 2026-08-04: Jeroen past het beginsaldo manueel aan en houdt het correct; de automatische afsluiting bevriest dus geen scheef saldo. Geen extra bevestiging nodig.

## 2026-08-04 — Token-migratie is groter geworden in plaats van kleiner · [debt]
- **Bevinding:** De finance-tokens zijn bewust uitgesteld, maar er zijn deze sessie componenten bijgekomen die opnieuw hardcoded kleuren gebruiken: `emerald-700` in de sectiebalk en de bullet charts, `--umanexChart4` en `--umanexPrimary500` semantisch ingezet in de waterfall, en `--umanexNeutral400` als arcering in de saldo-footer met een `// TODO` erbij. De lijst met token-paden, waardes en contrastcijfers staat in `briefings/2026-08-04-plan-advies-implementatie.md`.
- **Volgende zet:** Bij het aanmaken van de tokens in Tokens Studio meteen alle nieuwe componenten meenemen: `SectionBar`, `BalanceFooter`, `RunwayCard`, `WaterfallChart`, `BufferChart`, `VarianceChart` en `MonthVariance`. Grep op `emerald-`, `amber-` en `umanexChart` vindt ze allemaal.
- **Status:** resolved — 2026-08-05 in PR #194. Twee nieuwe primitive-ramps (`Success`, `Warning`) en een set `Semantic` met `Finance` en `Overlay`; veertien componenten stappen over en er staat geen paletklasse meer in de app. Het aanmaken gebeurde met de hand in `tokens.json` in plaats van via Tokens Studio — Jeroen pullt in de plugin zodat Figma dezelfde set krijgt. Uitwerking en de twee afwijkingen op het voorstel staan in `briefings/2026-08-05-tokens-finance-inventaris.md`.

## 2026-08-04 — Bullet charts en historie-vanaf-nu draaien ongezien op main · [onzekerheid]
- **Bevinding:** #172 en #173 zijn gemerged zonder dat `VarianceChart` ooit op scherm stond, en zonder visuele controle van het gedrag na `historyStartMonth`. De waterfall en de bufferopbouw zijn wél gezien en gefixt; juist dáár bleek dat een grafiek probleemloos compileert en toch 672px hoog wordt. Ongeteste gevallen: een waterfall met een negatief eindsaldo, en een categorie in de bullet charts die zo klein is dat de balk onzichtbaar wordt.
- **Volgende zet:** Met drie afgesloten maanden demodata naar `/analyse` kijken, in een tweede browserprofiel zodat de echte store ongemoeid blijft. Let op de bullet charts en forceer één maand met een negatief eindsaldo.
- **Status:** resolved — 2026-08-05: opgelost zonder de echte store aan te raken. `scripts/render-charts.tsx` rendert de grafieken met synthetische data naar een los HTML-bestand, inclusief de randgevallen die hier genoemd staan: een waterfall met een negatief eindsaldo, een bufferreeks die door nul zakt, achttien maanden labeldichtheid en de bullet charts. Drie echte fouten kwamen daarbij boven water (labels die aan het eind botsten, een spooklabel onder de nullijn, en een negatieve bufferstand die buiten beeld viel) — allemaal gefixt in PR #191. De "onzichtbaar kleine balk" bleek geen probleem: de bullet chart aggregeert per categorie en zet de bedragen als tekst ernaast.

## 2026-08-04 — Opslag, back-up en gebruik over meerdere browsers · [next-step]
- **Bevinding:** De store leeft in `localStorage` onder `cashflow-store-v3`, per browser en per profiel; deze sessie bleek dat concreet toen Chrome leeg was en Arc de echte cijfers had. Jeroen wil hierover nadenken vóór er gebouwd wordt. De keuze raakt drie dingen tegelijk: een back-up hebben, dezelfde data in meerdere browsers gebruiken, en of de app local-first blijft — beide adviesrapporten noemen dat laatste een expliciete vereiste.
- **Volgende zet:** De opties naast elkaar zetten met hun consequenties: (1) export/import als JSON-bestand, klein en volledig local-first, maar handmatig synchroniseren; (2) File System Access API met een gekozen bestand op schijf, automatisch en nog steeds lokaal, maar alleen in Chromium-browsers; (3) een echte backend zoals Supabase, waarmee synchroniseren vanzelf gaat maar de local-first vereiste vervalt en er accounts en beveiliging bij komen. De vorige entry over export is hierin opgegaan.
- **Status:** resolved — 2026-08-05: optie 3 gekozen en gebouwd (PR #181, main `4e5d568`). Eigen Supabase-project, puur remote: `localStorage` is geen bron meer, dus de local-first vereiste is bewust losgelaten in ruil voor synchroniseren zonder handwerk. Loginpoort was daarvan het onvermijdelijke gevolg — zonder sessie geeft RLS niets terug. De bestaande data is via `scripts/seed-supabase.mjs` uit een backup gemigreerd; 16 potten, 49 potbetalingen, 6 vaste uitgaven en 7 inkomsten kwamen identiek terug. Backups van de oude browserstores staan in `~/Documents/cashflow-backups/`.

## 2026-08-04 — Reviewbevindingen dagdiff: 21 open punten onder P0 · [debt]
- **Bevinding:** Een adversariële review over de volledige dagdiff (vier onafhankelijke brillen, elke bevinding daarna geprobeerd te weerleggen) leverde 27 beweringen op waarvan er 24 overeind bleven. De twee P0's zijn gefixt in PR #175. Wat resteert staat hieronder, ongewijzigd overgenomen zodat de context niet verdampt. Twee dingen vallen op als patroon: het snapshot-pad in de calculator stond volledig buiten het vangnet (noch `buffer-scenarios.ts` noch `calc-baseline.ts` geeft ooit een snapshots-argument mee), en de handgetekende SVG's rekenen nergens met negatieve waarden terwijl `potBalance` en `provisionThisMonth` dat wel kunnen worden.

- **[P1] `subtotals.ts:68`** — In de ankermaand telt `cashOverflow` de cash-poot van een potbetaling als kost, terwijl de pot-poot van diezelfde betaling behandeld wordt alsof ze al van het banksaldo af is (regel 95 trekt `paidFromPot` van de provisiekost af). Eén betaling, twee tegenstrijdige aannames — de cash-poot wordt dubbel geteld. De calculator zegt het zelf ook zo in de kop van het BESCHIKBAAR-blok (calculator.ts:257): cash-betalingen uit potten staan daar onder 'al effectief van het saldo weg'.
- **[P1] `subtotals.ts:95`** — Een betaling groter dan de potstand wordt nergens begrensd. In de ankermaand wordt de provisiekost negatief (`deferredFromPrevious + provisionThisMonth - paidFromPot` zonder ondergrens 0) en verschijnt er geld dat niet bestaat; in een latere maand (r. 96) telt alleen `provisionThisMonth`, waardoor het teveel-opgenomen bedrag nooit van het saldo gaat. De ReservationPaymentModal begrenst 'Uit pot' niet: syncInvoice cap't wel op availableSaldo, maar syncFromReservation/syncFromCash laten elke splitsing toe en handleSave valideert enkel res+cash === factuur.
- **[P1] `analysis.ts:44`** — `netBurn` leest `data.subtotals.costs`, maar elk snapshot wordt gebouwd als maand 0 (useCashflow.ts:195 roept calculateMonths met count 1 op de af te sluiten maand), en in de ankermaand sluit `costs` per definitie alles uit wat al betaald is. Hoe beter een afgesloten maand bijgehouden is, hoe lager de gemeten burn — bij een volledig afgerekende maand nul. De doc-comment bij de functie zegt 'wat eruit ging min wat erin kwam'; de implementatie meet 'wat er nog uit moet'.
- **[P1] `useCashflow.ts:18`** — cleanupStaleData verwijdert defers waarvan fromMonth én toMonth vóór de huidige maand liggen, en draait strikt vóór useAutoCloseMonth (finish() roept cleanup aan en zet daarna hydrated=true, wat de auto-close pas triggert). Een uitgestelde kost die in de vorige maand toekomt wordt dus weggegooid net vóór die maand bevroren wordt — de kost verdwijnt uit het snapshot én uit de hele voorwaartse keten.
- **[P1] `MonthCard.tsx:99`** — useDroppable staat op de MonthCard-root zonder `disabled`, ook wanneer de maand afgesloten is. De fieldset disabled={locked} beschermt alleen de formuliervelden binnenin (de sleepknop is een <button> en is dus inderdaad uitgeschakeld), maar de kolom blijft een geldig drop-target — inclusief de isOver-ring die de drop als geldig aankondigt.
- **[P1] `ReservationSection.tsx:84`** — De useEffect die localAmount synchroniseert mist isBudgetCurrentMonth in zijn deps, terwijl de nieuwe MonthNavigator die vlag nu wél kan omklappen op een gemounte MonthCard (key=monthKey, dus React hergebruikt de kolom bij navigatie en flipt enkel isFirst).
- **[P1] `MonthCard.tsx:165`** — De fieldset met disabled={locked} omvat ook de zuiver visuele 'Alle/Open'-filterknop van SectionBar; in een afgesloten maand zijn betaalde regels daardoor permanent onzichtbaar terwijl de sectiekop het volledige subtotaal blijft tonen.
- **[P1] `RunwayCard.tsx:35`** — filled wordt niet op 0 geklemd; bij een negatieve bufferstand levert dat een ongeldige CSS-breedte op, waarna de blok-div terugvalt op width:auto en de balk 100% vult — dus maximale runway toont bij een negatieve runway.
- **[P2] `calculator.ts:244`** — Een uitgestelde storting die toekomt wordt aan de pot gecrediteerd met `getEffectiveAmount(res)` — de settlement van de AANKOMSTmaand — terwijl dezelfde storting als kost geboekt wordt via `deferredReservationAmount`, dat `res.monthlyAmount` gebruikt (r. 223). Staat er in de aankomstmaand een settlement, dan krijgt de pot een ander bedrag dan er van het saldo af gaat.
- **[P2] `calculator.ts:342`** — Bij elke betaling met `fromCash > 0` wordt de potstand hard op 0 gezet ('de pot is volledig benut'), maar de doorrol (`nextDeferred`, r. 456-459) rekent het echte restant uit. Die aanname klopt alleen bij de auto-berekende splitsing; de modal laat de gebruiker vrij typen. Getoonde potstand en doorgerolde potstand lopen daarna permanent uit elkaar, en de bufferopname put uit de doorgerolde stand (`getDeferred`, r. 510), niet uit de getoonde.
- **[P2] `calculator.ts:85`** — `calcPotBalance` telt voor elke maand tussen startMonth en upToMonth een storting op en kent `reservationDefers` niet (staat niet eens in de signatuur). Een storting die weg-uitgesteld is uit een maand vóór het venster wordt daar dus meegeteld én bij aankomst binnen het venster nog eens gecrediteerd. Dit pad is het standaardpad: bij referenceMonth === anchorMonth (de default, beide currentMonth()) is `historicalPotBalances()` de bron van de startpotstanden.
- **[P2] `useCashflow.ts:182`** — useAutoCloseMonth bevriest de vorige maand ook wanneer de app die maand nooit gezien heeft. De hasData-check kijkt naar recurringItems.startMonth <= prevMonth en reservations.startMonth <= prevMonth — voorwaarden die altijd waar zijn zodra er ooit een vaste kost of spaarpot bestaat. De doc-comment redeneert bewust dat er maar één maand afgesloten wordt om geen reconstructie als historie vast te leggen, maar precies die ene maand ís een reconstructie zodra de gebruiker maanden oversloeg.
- **[P2] `calculator.ts:160`** — Bij het overnemen van een snapshot wordt potBalanceMap voor élke pot op snapshot.data pot.potBalance gezet, ook voor een maandelijks_budget. In de normale doorrol wordt een budget expliciet op 0 gezet (nextPotBalances.set(res.id, 0), regel 443). De eindstand van een budget (storting − betaald) lekt daardoor de maand ná een afgesloten maand in.
- **[P2] `BufferChart.tsx:50`** — Het y-domein loopt hard van 0 tot niceMax(max) zonder ondergrens, terwijl potBalance negatief kan worden; een negatieve bufferstand valt buiten de viewBox en verdwijnt geruisloos uit de grafiek.
- **[P2] `VarianceChart.tsx:122`** — De bullet chart kan een negatief 'werkelijk' bedrag niet tekenen: de balk krijgt een negatieve CSS-breedte, die de browser als ongeldig verwerpt, waardoor er helemaal geen balk verschijnt. Precies bij een bufferopname — het scenario van deze feature — verdwijnt de belangrijkste datapunt uit de grafiek.
- **[P2] `BalanceFooter.tsx:90`** — Het 'Beschikbaar'-bedrag heeft geen epsilon-guard, terwijl SectionBar die in dezelfde commit wél kreeg. Een eindsaldo dat door float-afronding op een verwaarloosbaar negatief getal landt, verschijnt als '€ -0,00' in de negatieve kleur — het meest prominente getal op het scherm meldt rood terwijl de maand exact op nul uitkomt.
- **[P3] `WaterfallChart.tsx:90`** — De tick-array [min, 0, max] wordt op waarde gekeyd terwijl 0 altijd in `values` zit, dus min ≤ 0 ≤ max; zodra min of max exact 0 is krijgen twee <g>-elementen dezelfde key.
- **[P3] `VarianceChart.tsx:122`** — De breedte (row.actual / max) * 100 kan negatief worden omdat provisionThisMonth bij een bufferopname negatief is; een negatieve percentage-breedte is ongeldige CSS en de absoluut gepositioneerde balk krimpt naar nul.
- **[P3] `WaterfallChart.tsx:90`** — De tick-array [min, 0, max] bevat in het normale geval twee keer dezelfde waarde 0, waardoor twee React-children dezelfde key krijgen en dezelfde rasterlijn plus astekst twee keer over elkaar getekend wordt.
- **[P3] `BufferChart.tsx:93`** — De aria-label van de SVG verwijst naar 'de tabel eronder', maar die tabel staat standaard niet in de DOM: showTable begint op false. De toggle-knop heeft geen aria-controls, dus er is geen programmatische relatie tussen de belofte en de knop die haar waarmaakt.
- **[P3] `BufferChart.tsx:178`** — De x-as tekent één label per punt zonder dunning, terwijl bufferSeries alle snapshots ooit teruggeeft. Naarmate de app maanden afsluit lopen de maandlabels over elkaar heen tot ze onleesbaar zijn.
- **Volgende zet:** Eerst het vangnet uitbreiden — snapshots meegeven in `calc-baseline.ts` en een scenario met een afgesloten maand in `buffer-scenarios.ts` — zodat de calculator-bevindingen (`subtotals.ts:68`, `subtotals.ts:95`, `calculator.ts:160/244/342`, `analysis.ts:44`) een test hebben om tegen te fixen. Daarna de SVG-randgevallen in één ronde: negatieve domeinen, gedeelde React-keys, en de bullet-balk die bij een negatieve waarde omvalt.
- **Status:** resolved — 2026-08-05: getrieerd tegen de huidige code (drie waren al opgelost of achterhaald door het buffer- en ankerwerk) en de resterende dertien opgelost in PR #189 (rekenkern), #190 (levensloop) en #191 (grafieken). Het vangnet dat de "volgende zet" vroeg staat er ook: `scripts/anchor-scenarios.ts` dekt het snapshot-pad en de ankerstaat, `buffer-scenarios.ts` groeide van 331 naar 465 checks.

