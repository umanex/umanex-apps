import { Meta } from '@storybook/addon-docs/blocks';
import { RadiusScale } from '../blocks/RadiusScale';

<Meta title="Tokens/Radius" />

# Radius

Eén mode-blinde rol, `radius`, in `Theme/base`. De preset leidt er drie utilities van af;
`rounded-lg` is de rol zelf, `-md` en `-sm` trekken er 2 en 4 px af.

<RadiusScale />
