# Token Mapping

> Gegenereerd vanuit `tokens.json` van het actieve project.
> Run `node skills/figma-naar-code/scripts/sync-tokens.js` vanuit de project root om te updaten.

Dit bestand wordt per klantproject gevuld. In umanex-os staat alleen dit template.
Zie klant-CLAUDE.md voor het pad naar `tokens.json` en de build output.
