# Bewust leeg. Hier stond `node-linker=hoisted` voor Metro, maar pnpm behandelt
# node-linker als een workspace-brede instelling — een per-package .npmrc wordt
# genegeerd, en npm/npx waarschuwt er bovendien over. Zie de root .npmrc voor hoe
# Metro nu werkt onder de geïsoleerde layout.
